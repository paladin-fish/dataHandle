declare type Type = 'pinyin' | 'word' | 'number' | 'wordnumber' | 'other';
interface Result {
    result: string;
    type: Type;
}
interface Option {
    /**
     * 解析拼音
     * @default true
     */
    pinyin?: boolean;
    /**
     * 解析单词
     * @default true
     */
    word?: boolean;
    /**
     * 解析数字
     * @default true
     */
    number?: boolean;
    /**
     * 解析单词数字的组合
     * @default true
     */
    wordnumber?: boolean;
    /**
     * 保留其他字符
     * @default true
     */
    other?: boolean;
    /**
     * 数量限制
     * @default 0 无限制
     */
    limit?: number;
}
/**
 * 汉子转换为拼音对象
 */
export declare function pinyinObj(str: string, opt?: Option): Result[];
/**
 * 字符串转换为拼音字符串
 * @param str
 * @returns
 */
export declare function pinyin(str: string, opt?: Option): string;
/**
 * 取得首字母
 * 注意：如果参数为字符串也只会返回首字母
 * @param str 字符
 */
export declare function pinyinInitial(str: string): string;
/**
 * 字符串提取拼音关键词
 * @param str
 * @returns 结果是去重的
 */
export declare function pinyinKeywords(str: string, opt?: Option): string[];
export {};
